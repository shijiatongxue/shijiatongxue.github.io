import React from 'react';

import Router from '../router';
import '../styles/main.scss';

export default function Main() {
    return (
        <main className="main">
            <Router />
        </main>
    );
}